{
 "condition_digest": "6b8330bee5a8718c",
 "condition_seed": 1,
 "dataset_id": "IDEALLab/photonics_2d_120_120_v1",
 "dataset_revision": "be811ff69d67cd8c79148e29f2a68d293ec00955",
 "engibench_version": "0.2.0+0a028c03d02b",
 "latent_instrument": {
  "algo": "constrained_plvae_2d",
  "config_fingerprint": "beb199bf",
  "expected_n_active": 32,
  "hf_entity": "IDEALLab",
  "hf_repo_prefix": "engiopt",
  "recon_only_config_fingerprint": "61f57b29",
  "recon_only_seed": 1,
  "seed": 1
 },
 "metrics": [
  "mmd",
  "pca_mmd",
  "dpp",
  "pixel_vendi",
  "novelty",
  "lv_mmd",
  "lv_coverage",
  "lv_vendi",
  "lv_residual",
  "lv_paired_distance",
  "lv_dual_gap",
  "lvoff_mmd",
  "lvoff_coverage",
  "lvoff_vendi",
  "lvoff_paired_distance",
  "gen_seconds",
  "params",
  "iog",
  "cog",
  "fog"
 ],
 "n_samples": 50,
 "notes": "Photonics2d has no volume budget (volume_condition is null), so the feasibility and condition families report themselves unavailable rather than producing a number. Latent instrument is beb199bf (performance constraint ON, rec threshold 0.15, decoder Lipschitz scale 10, predictor ratio 3.0), which compresses to 32 active dimensions at val NMSE 0.282 -- the best-reconstructing compressed arm on this problem. Photonics is the strongest case for latent metrics of the three 2D problems: R^2(conditions -> performance) is 0.56, so 44% of performance variance is design-attributable and a latent has something to encode beyond the conditions. THE COMPANION IS A KNOWN COMPROMISE. 61f57b29 was trained at reconstruction threshold 0.06, not beb199bf's 0.15, so lv_dual_gap on this problem conflates the performance constraint with a difference in reconstruction budget and must not be quoted as isolating the constraint. The matched-threshold alternative d54483bb is degenerate -- it puts 99% of its latent variance in a single dimension with sigma inflated ~40x, the trivial solution the decoder Lipschitz bound exists to prevent -- so the healthy mismatched arm was preferred over the matched collapsed one. Fixing this needs a recon-only arm trained at 0.15 that does not collapse; until then read lv_dual_gap here as indicative only. The earlier pin (0d2dd396 / 28a16723) predates the decoder fix of 4825cd7 and its packages no longer exist.",
 "objective_weight_condition": null,
 "objective_weights": null,
 "problem_conditions": [
  "lambda1",
  "lambda2",
  "blur_radius",
  "num_elems_x",
  "num_elems_y",
  "num_optimization_steps"
 ],
 "problem_id": "photonics2d",
 "sigma": 10.0,
 "version": "v1",
 "volfrac_tol": 0.01,
 "volume_condition": null
}
